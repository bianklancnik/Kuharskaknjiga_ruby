<h1>Recepti</h1>

<% @posts.each do |post| %>
    <div class="well">
        <h2><%= post.title %></h2>
        <div><%= post.body %></div>
        <%= link_to "Cel recept", post_path(post), :class => 'btn btn-default' %>
    </div>
<% end %>